---
const { props } = Astro
const { className } = props
---

<div {...props} class:list={['relative px-4 sm:px-8 lg:px-12', className]}>
  <div class="mx-auto max-w-2xl lg:max-w-5xl">
    <slot />
  </div>
</div>
